<Page x:Class="chel.FilmPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Films">
    <ScrollViewer>
        <ItemsControl x:Name="MoviesItemsControl">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#DDD" BorderThickness="1" Margin="6" Width="220" Padding="8" CornerRadius="6">
                        <StackPanel>
                            <Image Width="200" Height="300" Stretch="UniformToFill" Source="{Binding CoverPath}"/>
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" Margin="0,6,0,0" TextWrapping="Wrap"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock Text="Рейтинг: " />
                                <TextBlock Text="{Binding Rating}" FontWeight="SemiBold"/>


                            </StackPanel>
                            <Button Content="Перейти" Margin="0,8,0,0" Click="OpenFilm_Click" Tag="{Binding MovieId}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Page>
